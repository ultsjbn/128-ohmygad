import { Typography } from "@/components/typography";
import { Paper } from "@/components/paper";

export default function EventsPage() {
    return (
        <Paper elevation="elevated" title="Events" titleVariant="heading-2">
            <Typography variant="body-1-median">Events go here</Typography>
        </Paper>
    );
}